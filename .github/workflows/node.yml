name: Node Workflow

on: pull_request

jobs:
  lint:
    name: 'Check the repo'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3

      - run: npm ci
      - run: npm run lint
